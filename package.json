{
  "name": "nlab-nav-home",
  "version": "1.0.0",
  "private": true,
  "description": "NLab 实验室课题组服务总导航主页",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "preview:local": "vite preview",
    "check": "tsc --noEmit && vite build",
    "deploy:static": "bash scripts/deploy-static.sh"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
